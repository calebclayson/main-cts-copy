var arr = ['1', 2, 'John'];

arr.length; //returns number of items in the array

arr.pop(); //removes last item from array

arr.push('bud'); //adds to the end of the array

var elementPopped = arr.pop(); //stores the popped value into the variable

arr.shift(); //takes off the value from the beginning of the list

arr.unshift('3'); //adds value to the beginning of the array